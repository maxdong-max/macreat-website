# 2026-04-04 工作日志

## Vercel 部署问题

### 错误原因
- `workspace-shrimp-publish` 项目部署失败
- 错误信息: `Invalid vercel.json: The route rule "/(.*)" uses the "output" property, which is not allowed for Next.js projects. Use "destination" instead.`
- 原因: `vercel.json` 中错误使用了 `output` 属性

### 修复方案
- 已修改 `vercel.json`，移除错误的 `output` 配置
- 修改后的配置保留 `buildCommand`, `installCommand`, `framework`, `outputDirectory`

### 待办
- 提交 vercel.json 修改并推送到 GitHub
- 重新触发 Vercel 部署

### GitHub 仓库
- https://github.com/maxdong-max/macreat-website
- 最新 Commit: fe5fe68 (Fix: disable Railway API rewrites)

### 修复记录
- 禁用 next.config.js 中的 Railway API rewrites（后端不可用导致错误）
- 推送 commit fe5fe68 到 GitHub，触发 Vercel 自动部署